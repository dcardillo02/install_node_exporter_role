---
# tasks file for install_node_exporter

- name: Check if node exporter exist 
  stat: 
    path: "{{ node_exporter_bin }}"
  register: __check_node_exporter_present

- name: Create node exporter user
  user:
    name: "{{ node_exporter_user }}"
    append: true
    shell: /usr/sbin/nologin
    system: true
    create_home: false

- name: Create node exporter config dir
  file:
    path: "{{ node_exporter_dir_conf }}"
    state: directory
    owner: "{{ node_exporter_user }}"
    group: "{{ node_exporter_group }}"

- name: if node exporter exist get version
  shell: "cat /etc/systemd/system/node_exporter.service | grep Version | sed s/'.*Version '//g"
  when: __check_node_exporter_present.stat.exists == true
  changed_when: false
  register: __get_node_exporter_version

- name: Download and unzip node exporter if not exist
  unarchive:
    src: "https://github.com/prometheus/node_exporter/releases/download/v{{ node_exporter_version }}/node_exporter-{{ node_exporter_version }}.linux-amd64.tar.gz"
    dest: /tmp/
    remote_src: yes
    validate_certs: no

- name: Move the binary to the final destination
  copy:
    src: "/tmp/node_exporter-{{ node_exporter_version }}.linux-amd64/node_exporter"
    dest: "{{ node_exporter_bin }}"
    owner: "{{ node_exporter_user }}"
    group: "{{ node_exporter_group }}"
    mode: 0755
    remote_src: yes
  when: __check_node_exporter_present.stat.exists == false or not __get_node_exporter_version.stdout == node_exporter_version

- name: Clean
  file:
    path: /tmp/node_exporter-{{ node_exporter_version }}.linux-amd64/
    state: absent

- name: Add node exporter port to firewall
  firewalld:
    port: 9100/tcp
    permanent: yes
    state: enabled

- name: Reload firewall after changes
  systemd:
    name: firewalld.service
    state: reloaded
    
- name: Install service
  template:
    src: node_exporter.service.j2
    dest: /etc/systemd/system/node_exporter.service
    owner: root
    group: root
    mode: 0755
  notify: reload_daemon_and_restart_node_exporter
- meta: flush_handlers

- name: Service always started
  systemd:
    name: node_exporter
    state: started
    enabled: yes



# - name: Download_NodeExporter
#   ansible.builtin.get_url:
#     url: '{{remote_node_exporter_install_path}}'
#     dest: ~/
# - name: Untar_NodeExporter
#   ansible.builtin.unarchive:
#     src: node_exporter-1.4.0.linux-amd64.tar.gz
#     dest: ~/
#     remote_src: yes
# - name: Run_NodeExporter
#   ansible.builtin.shell:
#     chdir: ~/node_exporter-1.4.0.linux-amd64
#     cmd: nohup ./node_exporter &
